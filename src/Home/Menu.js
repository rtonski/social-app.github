import React from "react";
import "./Menu.css";

const Menu = () => {
  setTimeout(() => prompt("nanananana", "mammaa"), 10000);

  return (
    <div>
      <button>Home</button>
      <button>Login</button>
      <button>Sign Up</button>
    </div>
  );
};

export default Menu;
